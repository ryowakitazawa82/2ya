- var googleFontPreConnectURL = 'https://fonts.gstatic.com';
- var googleFontURL = 'https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300;400;500;700&display=swap';
- var fontAwesomeURL = 'https://kit.fontawesome.com/4448fb714f.js';
- var lottieActivateURL = 'https://cdnjs.cloudflare.com/ajax/libs/bodymovin/5.5.9/lottie.min.js';

doctype html
html(lang='ja')
  head
    // Google Tag Manager
    script.
      (function (w, d, s, l, i) {
        w[l] = w[l] || [];
        w[l].push({
          'gtm.start': new Date().getTime(),
          event: 'gtm.js',
        });
        var f = d.getElementsByTagName(s)[0],
          j = d.createElement(s),
          dl = l != 'dataLayer' ? '&l=' + l : '';
        j.async = true;
        j.src = 'https://www.googletagmanager.com/gtm.js?id=' + i + dl;
        f.parentNode.insertBefore(j, f);
      })(window, document, 'script', 'dataLayer', 'GTM-T2N7HKV');
    // End Google Tag Manager
    meta(charset='utf-8')
    meta(http-equiv='x-ua-compatible', content='ie=edge')
    title 2YearAnniversary
    meta(name='description', content='2YearAnniversary')
    meta(
      name='viewport',
      content='width=device-width,initial-scale=1.0,minimum-scale=1.0,maximum-scale=1.0,user-scalable=no'
    )
    meta(name='format-detection', content='telephone=no')
    // OG
    meta(property='og:type', content='website')
    meta(property='og:title', content='2YearAnniversary')
    meta(property='og:description', content='2YearAnniversary')
    // favicon
    link(rel='shortcut icon', href='')
    // GoogleFonts
    link(rel='preconnect', href=googleFontPreConnectURL)
    link(href=googleFontURL, rel='stylesheet')
    // font awesome
    script(src=fontAwesomeURL, crossorigin='anonymous')

    // style sheets
    link(rel='stylesheet', href='./assets/css/style.min.css')
  body
    header
      block header
        // ヘッダー固有の内容をここに追加
    main
      block content
        // ページ固有の内容をここに追加
    footer
      block footer
        // フッター固有の内容をここに追加

    // 共通で読み込むスクリプト(ページ固有のスクリプトはpages配下で読み込む)
    script(src='./assets/js/common/scrollObserver.js')
    script(src='./assets/js/common/common.js')
